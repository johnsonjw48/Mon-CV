import React from 'react';
import Navigation from '../Components/Navigation';

const Acceuil = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Acceuil;